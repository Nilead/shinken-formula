# -*- mode:yaml -*-

shinken:
  #TODO: use "worker" to match our salt states
  poller:
    tags: DMZ pinger
    realm: All

  # default snmp community
  snmp_community: public


  primary:
    config_path: salt://whatevs
    graphite_host: foo

  # where to get shinken configuration from
  config: salt://shinken/config

  #lookups:

